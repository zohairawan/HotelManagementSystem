//TODO: Currently user can only perform one function when running the program. Change it so they can run multiple functions

public class Main {

    public static void main(String[] args) {
        Manager manager = new Manager();
        manager.startMenu();
    }
}
